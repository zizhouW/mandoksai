import React, { useState } from 'react';
import PageContainer from '../components/page-container/PageContainer';

const Random = () => (
  <PageContainer>its me</PageContainer>
);

export default Random;
